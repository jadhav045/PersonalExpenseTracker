<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f5f5f5;
        }

        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
        }

        /* Center the form */
        .form-container {
            background-color: white;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            text-align: center;
            margin-top: 100px; /* Ensures form is below the navbar */
            margin-bottom: auto;
        }

        .form-header {
            margin-bottom: 20px;
        }

        .form-header h1 {
            font-size: 1.75rem;
            font-weight: bold;
            margin-bottom: 10px;
            color: white;
            background: linear-gradient(90deg, #6a11cb, #2575fc);
            padding: 15px;
            border-radius: 10px;
        }

        .form-group {
            position: relative;
            margin-bottom: 20px;
        }

        .form-group input {
            width: 100%;
            padding: 12px 20px;
            font-size: 1rem;
            border: 1px solid #ccc;
            border-radius: 10px;
            outline: none;
            transition: border-color 0.3s ease;
        }

        .form-group input:focus {
            border-color: #6a11cb;
        }

        /* Custom Checkbox */
        .form-check {
            text-align: left;
            margin-bottom: 15px;
        }

        .form-check label {
            font-size: 0.9rem;
        }

        .form-check input {
            margin-right: 10px;
        }

        /* Button Styling */
        .btn-custom {
            display: block;
            width: 100%;
            padding: 12px;
            background: linear-gradient(90deg, #6a11cb, #2575fc);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .btn-custom:hover {
            background: linear-gradient(90deg, #5b10c2, #2068f2);
        }

        .text-center {
            text-align: center;
        }

        /* Link Styling */
        a {
            color: #2575fc;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        .additional-links {
            margin-top: 15px;
        }

        footer {
            background-color: #f8f9fa;
            padding: 20px;
            text-align: center;
            margin-top: auto;
        }

        /* Push content down from fixed navbar */
        .content {
            padding-top: 70px;
            flex-grow: 1;
        }

    </style>
</head>
<body>

    <!-- Fixed Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/auth/login">
            <img src="https://media.istockphoto.com/id/1296117554/vector/finance-management-rgb-color-icon.jpg?s=612x612&w=0&k=20&c=xmx_1sMrNg_Z1JC6jFyES5ecw8NZ5Tvm6p3LH9vN-vg=" alt="Gullak" height="30"> 
            Gullak
        </a>
        <div class="collapse navbar-collapse justify-content-end">
            <ul class="navbar-nav">
                
            </ul>
        </div>
    </nav>

    <!-- Main content area -->
    <div class="content d-flex justify-content-center align-items-center">
        <div class="form-container">
            <div class="form-header">
                <h1>Login Form</h1>
            </div>
            <form action="/auth/login" method="POST">
                <div class="form-group">
                    <input type="email" id="email" name="email" required placeholder="Email Address">
                </div>
                <div class="form-group">
                    <input type="password" id="password" name="password" required placeholder="Password">
                </div>
                <div class="form-check">
                    <input type="checkbox" id="remember" class="form-check-input">
                    <label for="remember" class="form-check-label">Remember me</label>
                </div>
                <button type="submit" class="btn-custom">Login</button>
            </form>

            <div class="additional-links text-center">
                <p>Not a member? <a href="/auth/register">Signup now</a></p>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-light text-center py-3 mt-5">
        <p>&copy; 2024 Gullak</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
